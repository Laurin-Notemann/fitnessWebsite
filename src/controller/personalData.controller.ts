import { fitnessDB } from "../connection/connection";
import { PersonalData } from "../models/PersonalData.model";

export class PersonalDataController {
    static addPersonalData = (weight: number, height: number) => {

    }
    static getPersonalDataByID = (id:number) => {

    }
    static updatePersonalDataByID = (id: number, weight: number, height: number) => {

    }
    static deletePersonalDataByID = (id: number) => {

    }
}